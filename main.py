from sys import argv
form sys import exit

script, score_file = argv

if score_file == null:
    print("Please run the program again, with the file to use\nas an argument for the program.")
    exit(0)
else:

